LOCODE NEXT-CODEX HANDOFF
=========================

Created: 2026-07-18 (Asia/Colombo)
Workspace: E:\RP-SE-009\ai agent app
Repository state: dirty working tree containing the user's intended Locode work plus possibly unrelated user changes. Preserve everything. Do not git reset/checkout/revert broadly.

USER'S REAL PRIORITY
--------------------
The user's highest priority is speed plus first-published-code correctness. Planning may miss one or two minor ideas, but generated code must ideally reach the final gates/build without bugs on the first invocation. Do not use fixed product/page templates. The app structure, pages, behavior, copy, and design must come from the complete user input. Deterministic framework/auth/CRUD/contracts/shadcn infrastructure is allowed.

FINAL MODEL ROUTING REQUEST
---------------------------
1. Architect/planning and structured target selection: nemotron-3-super:cloud
   - 262,144-token context
   - thinking OFF
2. Complete-file code generation, update, and repair: local gemma4:12b
   - 98,304-token context
   - Q4_K_M
   - thinking OFF
   - temperature 0 for code
   - maximum two dependency-wave calls concurrently
   - Flash Attention + q8_0 KV cache
   - must be 100% GPU before staging/project writes
3. No Gemini, no local/cloud fallback. Retry only the assigned model and fail clearly.
4. Legacy model request fields remain accepted for compatibility but cannot override routing.

FULL-CONTEXT / MEMORY CONTRACT
------------------------------
- .locode/product-context.json is the sole authoritative LLM memory, version product-context/v1.
- It contains exact raw input, normalized spec, design plan, generation plan, route/API/field contract, declared components, exact available shadcn exports, and stable contextHash.
- Every post-architecture prompt receives the complete canonical product context; update/repair additionally receives the complete affected file and exact dependency signatures. Never silently truncate canonical context.
- .locode/*.md files are derived human-readable views, not duplicate prompt authorities.
- memory-manifest.json contains context hash plus SHA-256 and byte count of every memory document.
- PROGRESS.md remains append-only.
- Older projects without valid product-context/v1 must fail update/repair with a regenerate/import-context message.

IMPORTANT NEW WINDOWS MEMORY FIX (COMPLETED THIS SESSION)
---------------------------------------------------------
Found a real Windows-only integrity bug: Memory._write_manifest() read documents as text, which normalized CRLF to LF before hashing. validate_memory() used the same normalized view, so internal validation passed even though manifest SHA-256 did not match actual disk bytes.

Fixed agents/memory.py to:
- hash exact read_bytes() content;
- record exact persisted byte length;
- validate exact persisted bytes and byte count;
- decode only for context marker checks;
- compare the first marker line in a CRLF-safe way.

Extended tests/test_gemma_runtime.py to assert every manifest digest and byte count against actual read_bytes().

Regression result after the fix:
79 passed in 1.66s

Refreshed the existing TeaGuide memory manifest. Exact disk verification result:
API.md OK
COMPONENTS.md OK
CONTRACT.md OK
ENTITIES.md OK
LOCATIONS.md OK
MODELS.md OK
PROGRESS.md OK
PROJECT.md OK
ROUTES.md OK
TASKS.md OK
product-context.json OK
version = product-context/v1
manifest contextHash equals product contextHash
byte_exact_failures = []

MAJOR ENGINE WORK ALREADY IMPLEMENTED
-------------------------------------
Architect/input-driven planning:
- agents/architect.py is the dedicated Architect; RefinerAgent remains a compatibility wrapper.
- Architect produces normalized auth, roles, design, pages, data_model, generation_plan, and exact component dependencies.
- Input-driven plans replace fixed page/auth/role/design synthesis.
- Validation covers unique routes, referenced roles/resources, field types, auth consistency, declared page functions, explicit page counts, and action authorization.
- Validation errors go back to Nemotron for at most two corrections. There is no generic template fallback.
- Component dependency names are normalized to component paths.
- auth_enabled() is controlled only by auth.enabled.
- Explicit page-count phrases one..ten/N-page are enforced.
- Bookmark actions are recognized as persistent action authorization.
- Low-risk share is no longer treated as an explicit-only high-impact action. Approve/assign/book/delete/download/export/import/invite/pay/print/upload remain strict.

Canonical context/memory:
- agents/product_context.py builds and validates product-context/v1.
- agents/memory.py writes derived Markdown views and byte-exact manifest.
- Complete raw input/spec/design/roles/pages/models/routes/API/UI export map/hash is included in prompts.
- Context fitting reserves output tokens and fails rather than truncating.

Model/runtime:
- agents/llm.py contains the Nemotron planner + Gemma generator split, no fallback, think=false, truncation rejection, and runtime checks.
- Gemma is prewarmed/kept alive; installed model/context/GPU offload is checked before project writes.
- UI model selectors were replaced by read-only runtime indicators.

Generation/preflight:
- agents/gen_agents.py uses full prompt ordering, complete-file contracts, deterministic per-file seeds, expected-size budgets, exact dependency signatures, staging preflight, and one bounded SEARCH/REPLACE micro-repair.
- Published project is only replaced after staged module/contract/TypeScript/build gates are green.
- Failed candidate source + diagnostics are preserved in .locode/preflight-failures.
- Implemented deterministic corrections for proven recurrent faults:
  * missing shadcn named imports;
  * missing canonical DTO type imports;
  * invalid Lucide import names using exact TypeScript suggestions;
  * exact local identifier replacements from compiler "Did you mean" diagnostics;
  * literal/fixed Tailwind colors converted to semantic tokens;
  * public reference first-load fallback content;
  * unwired filter state and missing controlled filter props;
  * actual resource-field filtering;
  * card selection state wired into detail panels;
  * one-route topnav links converted to real section anchors + IDs;
  * decorative absolute overlays get pointer-events-none;
  * common malformed JSX closes/unclosed ancestors/surplus braces;
  * malformed multiline template-string className endings;
  * explicit appended final reimplementations replace earlier broken duplicates;
  * common mojibake normalization.

Quality/contract gates:
- agents/quality.py detects nonsemantic colors, missing aria-pressed, missing public fallback, unwired filters/selections, useless one-route nav links, pointer-intercepting overlays, mojibake, dead routes/APIs.
- agents/repair/contract_scan.py fixed a false positive where a single-letter callback parameter such as t matched the tail of identifiers like React/result; identifier boundaries are now enforced.

UI stack:
- Generated UI uses Tailwind + audited shadcn registry + Lucide only.
- No generated MUI/Emotion dependencies/imports/providers.
- Exact shadcn module/export signatures are included in context.
- Registry includes the common primitives, Radix Select, Separator orientation, and Dialog asChild support.
- Semantic CSS variables/tokens drive palette, typography, radius, shadow, spacing, mode, motion, and nav style.

CURRENT TEST STATUS
-------------------
Command:
  cd "E:\RP-SE-009\ai agent app"
  python -m pytest -q

Latest result:
  79 passed

TEAGUIDE ACCEPTANCE APP (COMPLETED)
----------------------------------
Published app:
  E:\RP-SE-009\ai agent app\production-ready\teaguide

Backup retained intentionally:
  E:\RP-SE-009\ai agent app\production-ready\.teaguide.smoke-backup

Input was a one-page no-account specialty tea comparison app with five exact tea types, caffeine filter, selection, and recipe copy.

Evidence:
- All generated files passed preflight.
- Static/module/contract gates clean.
- Final next build GREEN on its first invocation after publication.
- Attractive warm editorial semantic-token UI.
- Real Home/Guide/About anchor navigation.
- Exactly five curated tea cards.
- Low caffeine filter changed five cards to one and aria-pressed became true.
- Selecting Silver Needle opened the correct recipe.
- Copy action showed "Copied!".
- Browser-session clipboard API returned empty, but Windows Get-Clipboard -Raw contained the full Silver Needle recipe, proving copy worked.
- Browser console warnings/errors: []
- At 1280px: no horizontal overflow; responsive grid classes include grid-cols-1 md:grid-cols-2 lg:grid-cols-3.
- Exact 375px browser viewport override timed out/reset the browser control kernel twice. This was a browser-control limitation, not an app exception. Do not repeatedly retry the same failing viewport capability; validate responsive classes/source and test the default live viewport.

Important server lesson:
- Do not use `next dev` after a staging directory was renamed. Turbopack can panic with "Next.js package not found" and return HTML without hydration, making clicks look broken.
- For acceptance browser testing, run `npm run build`, then `next start` on a free port.
- Stop the exact tracked server process tree before renaming or regenerating the same target. Do not kill unrelated Node processes.

LATEST AFTERGLOW GENERATION (FAILED SAFELY; NOT PUBLISHED)
----------------------------------------------------------
Exact input:
  Create “Atlas of Afterglow,” a five-page public cultural travel journal with no accounts. Give it a real top navigation and five purposeful routes: Home, City Stories, one Story Detail, Field Notes, and About. Use a bold editorial identity with deep ink, warm saffron, expressive serif typography, layered CSS-gradient artwork, and no external image dependency. Visitors can search stories, filter them by region and mood, bookmark stories locally, copy a short three-stop itinerary, and submit a field-note contact form with clear client-side validation plus loading, success, empty, and error states. Every page must contain specific travel-journal copy and curated first-load content. Make the layouts mobile-first and structurally distinct. Do not add accounts, dashboards, fake metrics, testimonials, generic entities, or dead links.

Original command:
  python scripts\gen_smoketest.py afterglow-static-live2 '<the exact input above>'

Final attempt status:
- Python generator PID 23588 exited with code 1 after a safe preflight failure.
- Staging directory:
  E:\RP-SE-009\ai agent app\production-ready\.atlasofafterglow.smoke-staging
- No published atlasofafterglow directory yet.
- Runtime at generation time:
  gemma4:12b, Q4_K_M, 8.5 GB, 100% GPU, context 98304
- Architect completed and staging was created only after runtime verification.
- Core emitted 39 deterministic files.
- Architect planned 18 generated files: 5 pages, 8 semantic feature components, 0 separate logic files, 5 valid routes.
- Infrastructure preflight reported 8 typed files clean with TypeScript 5.9.3.
- Deterministic types/models/validators/APIs/shadcn primitives remain staged.
- The first component wave found the two candidate issues listed below and entered the single bounded micro-repair path.
- The Hero repair was not the terminating exception. StoryCard repair exhausted its 1,400 output-token budget.
- agents.llm.LLMTruncatedError correctly rejected the incomplete repair response. It was not written and nothing was published.

Exact terminating error:
  Gemma response exhausted its 1400 output tokens; partial content=14 chars, thinking=0 chars, stopped near '```thought ```'; the incomplete result was rejected and will not be written.

This reveals a new engine-level fault to fix before rerunning:
- The bounded micro-repair output budget is too small or incorrectly assigned for a malformed 96-line component with 20 cascading JSX diagnostics.
- Thinking was reported as zero, but the tiny partial response was literally ```thought ```. Inspect Ollama/Gemma response parsing and prompt behavior as well as budget allocation; do not merely remove truncation rejection.
- Preserve the no-partial-write behavior.
- Prefer a bounded complete-file or targeted SEARCH/REPLACE repair budget derived from affected file size and diagnostic scope. A 1,400-token universal repair cap is not sufficient for this case.
- Add regression coverage proving a repair response cannot be truncated and that a file around this size receives enough output budget while still rejecting done_reason=length.

The original unified exec output cell was ID 14 in the previous Codex task and has completed. Before rerunning, still verify there is no surviving generator process:
  Get-Process python -ErrorAction SilentlyContinue
  ollama ps
  Get-ChildItem production-ready -Force | Where-Object { $_.Name -like '*afterglow*' }
  Get-ChildItem smoke-reports -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -like '*afterglow*' }

Expected current state is only .atlasofafterglow.smoke-staging and no published atlasofafterglow. Inspect preserved diagnostics, fix the general repair-budget/parser fault, run tests, then rerun the same smoke only after confirming no generator process owns the staging directory.

Current captured preflight evidence (not published):
1. components/features/home/HeroSection.tsx candidate:
   code = nonsemantic-hex-color
   message = generated UI must use semantic theme tokens, not literal hex colours
2. components/features/stories/StoryCard.tsx candidate:
   malformed JSX diagnostics including TS2657, TS1003, TS1382, TS1002, TS17002, TS1005, TS1109, TS1128 around lines 36-96.

These prove preflight prevented broken code from publication. Inspect:
  E:\RP-SE-009\ai agent app\production-ready\.atlasofafterglow.smoke-staging\.locode\preflight-failures

Convert only a proven, general recurrent cause into an engine correction + regression test. Do not hand-patch only the generated app and call the engine fixed.

REMAINING ACCEPTANCE WORK, IN ORDER
-----------------------------------
1. Finish/observe the current five-page Afterglow generation.
2. If it fails, inspect exact preflight artifacts, fix the general engine fault, add regression coverage, rerun 79 tests, then rerun Afterglow.
3. Require clean staged gates and green next build before publication.
4. Live-test Afterglow using `next start` (not `next dev`) on a free port:
   - all five declared routes load;
   - top navigation and all links are real;
   - search works;
   - region and mood filters work;
   - empty/error/loading states exist and behave;
   - local bookmark persists in localStorage;
   - itinerary copy works (verify system clipboard if browser API reads empty);
   - Field Note form validation, loading, and success work;
   - no browser console errors/warnings;
   - no horizontal overflow and responsive classes/source are valid;
   - design/copy/routes are specific and structurally distinct.
5. Turn every encountered reproducible generation bug into a general engine prevention and test.
6. Generate a natural approximately ten-page authenticated multi-role app.
7. Live-test roles, route guards, forms, design, CRUD states, responsive behavior, console, and build.
8. Run the full pytest suite again and summarize timings/tokens/clean rate/preflight corrections/final gates.

SUGGESTED AUTH/MULTI-ROLE INPUT
-------------------------------
Use this after Afterglow is fully done:
  Create “Harborlight Studio,” a collaborative community arts residency platform with three roles: artist, curator, and coordinator. Use account login; artists may self-sign up, while curators and coordinators are invite-only. Build about ten purposeful product routes: a public landing page, artist dashboard, artist profile, proposal list, new proposal form, proposal detail, curator review queue, curator review detail, coordinator schedule and venues, and shared notifications/settings as needed to keep the product near ten routes. Login and signup may be deterministic auth routes outside the product-page count. Artists create, edit, and submit proposals; curators review, approve, or request changes with notes; coordinators assign venues and schedule residency dates. Use a warm modern studio identity, sidebar navigation inside authenticated areas, clear role guards, responsive forms/tables/cards, and functioning loading, empty, error, and success states. Do not add fake metrics, generic entities, dead links, or a fixed dashboard template.

Before this smoke, inspect existing auth conventions/tests so that product page count does not conflict with deterministic /login and /signup scaffold routes. If the prompt says exact ten-page, the Architect's page-count validator will enforce exactly ten normalized pages.

BROWSER SKILL REQUIREMENT FOR NEXT ACCOUNT
------------------------------------------
The user explicitly requested live browser testing. Before browser actions, announce use of the browser skill and read the current complete skill file:
  C:\Users\ravin\.codex\plugins\cache\openai-bundled\browser\26.715.31925\skills\control-in-app-browser\SKILL.md

The version changed after the power cut. Follow that version exactly. Use only its prescribed mcp__node_repl__js browser mechanism; do not launch standalone Playwright. Treat the previous browser kernel as lost after the PC shutdown. At the very end, follow the skill's tab finalization rules.

WORKTREE / FILES
----------------
Modified tracked files at handoff time:
  agents/auth_scaffold.py
  agents/bugfix_agent.py
  agents/builder.py
  agents/component_registry.py
  agents/core.py
  agents/design/planner.py
  agents/gen_agents.py
  agents/llm.py
  agents/memory.py
  agents/orchestrator.py
  agents/quality.py
  agents/refiner.py
  agents/repair/contract_scan.py
  agents/repair/harness.py
  agents/rules/component.md
  agents/rules/section.md
  agents/scaffold.py
  contributing.md
  electron/main.cjs
  electron/ui/index.html
  pipeline.py
  scripts/gen_smoketest.py
  server.py
  setup.sh
  tests/test_multipage.py
  tests/test_rolewise.py
  ui/index.html

Untracked intended additions:
  agents/architect.py
  agents/product_context.py
  tests/test_architect_context.py
  tests/test_contract_scan.py
  tests/test_gemma_runtime.py

Use apply_patch for edits. Preserve unrelated user changes. No git commit/push was requested.

HANDOFF COMPLETION RULE
-----------------------
Do not claim the overall request complete until both Afterglow and Harborlight are generated, final gates/build are green, live flows are tested as far as the local environment allows, recurring bugs are prevented at engine level with tests, memory hashes are exact, and the final regression suite is green.
