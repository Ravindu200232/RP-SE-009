# Optional extraction dependencies. Install for full PDF/image/voice support.
#   pip install -r requirements-extract.txt
pypdf==5.0.1
pdfplumber==0.11.4
pillow==10.4.0
pytesseract==0.3.13          # requires the Tesseract binary on PATH
# easyocr==1.7.2             # heavier OCR fallback (pulls torch); uncomment if needed
# faster-whisper==1.0.3      # local speech-to-text; uncomment to enable voice
