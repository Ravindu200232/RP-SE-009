---
name: stack-testing
description: "The skills for Unit tests and browser journeys. Read this index first, then read only the entries the task actually needs with readSkill resourcePath."
---

# Unit tests and browser journeys

Everything here is read with `readSkill` and a `resourcePath`, for example
`readSkill(name="stack-testing", resourcePath="vitest-unit-testing.md")`.

Read the entry that matches what you are about to write, not all of them.

- **`vitest-unit-testing.md`** — Write fast unit tests with Vitest, coverage reporting, mocking, and snapshots. Apply when testing utilities, components, services, or building test co
- **`testing.md`** — Apply TDD with RED-GREEN-REFACTOR cycles, separate unit tests from integration tests, ensure comprehensive coverage. Apply when writing tests, evaluat
- **`tdd-workflow.md`** — Enforce strict Test-Driven Development with Red-Green-Refactor discipline. Apply when implementing new features, fixing bugs, or refactoring code. Ens
- **`build-error-resolver.md`** — Rapidly fix build failures, type errors, and lint issues with minimal diffs. Apply when builds fail, TypeScript reports errors, or CI/CD pipelines bre
- **`code-review.md`** — Systematically review PRs for security, testing, code quality, accessibility. Apply when reviewing pull requests, auditing endpoints, checking securit
