/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing here by default. Every option added without a reason is a
  // behaviour difference between this project and the documentation.
};

export default nextConfig;
