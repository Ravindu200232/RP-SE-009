{
  "purpose": "Email and SMS",
  "question": "How should the app send messages?",
  "choices": [
    {
      "id": "log-only",
      "label": "Test — write them down, send nothing",
      "hint": "The whole flow runs and every message is recorded, so nothing reaches a real person while you build."
    },
    {
      "id": "resend-sandbox",
      "label": "Resend — sandbox sender",
      "hint": "Real sending with no domain set up. Uses onboarding@resend.dev, which only delivers to the address that owns the Resend account.",
      "fields": [
        {"key": "RESEND_API_KEY", "label": "Resend API key",
         "example": "re_A1b2C3d4_E5f6G7h8J9k0LmNoPqRsTu",
         "hint": "resend.com → API Keys → Create. A send-only key is enough.",
         "secret": true},
        {"key": "EMAIL_FROM", "label": "Sender address",
         "example": "onboarding@resend.dev",
         "hint": "Resend's own sandbox sender. Leave it as it is until you have verified a domain.",
         "secret": false}
      ]
    },
    {
      "id": "resend-domain",
      "label": "Resend — your own domain",
      "hint": "Sends to anyone. Needs a domain verified at resend.com/domains first, or every send is refused.",
      "fields": [
        {"key": "RESEND_API_KEY", "label": "Resend API key",
         "example": "re_A1b2C3d4_E5f6G7h8J9k0LmNoPqRsTu", "secret": true},
        {"key": "EMAIL_FROM", "label": "Sender address",
         "example": "orders@yourdomain.com",
         "hint": "Must be at a domain that is verified in Resend.", "secret": false}
      ]
    },
    {
      "id": "twilio-sms",
      "label": "Twilio — SMS",
      "hint": "Text messages. Numbers are stored in E.164: +94771234567.",
      "fields": [
        {"key": "TWILIO_ACCOUNT_SID", "label": "Account SID",
         "example": "AC1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d",
         "hint": "console.twilio.com, at the top of the page.", "secret": false},
        {"key": "TWILIO_AUTH_TOKEN", "label": "Auth token",
         "example": "9f8e7d6c5b4a39281706f5e4d3c2b1a0",
         "hint": "The same panel, behind 'show'.", "secret": true},
        {"key": "TWILIO_FROM_NUMBER", "label": "Sending number",
         "example": "+15005550006",
         "hint": "Phone Numbers → Manage. +15005550006 is Twilio's magic test number.",
         "secret": false}
      ]
    }
  ]
}
