---
name: stack-mern
description: "The skills for React (Vite) + Express microservices + MongoDB. Read this index first, then read only the entries the task actually needs with readSkill resourcePath."
---

# React (Vite) + Express microservices + MongoDB

Everything here is read with `readSkill` and a `resourcePath`, for example
`readSkill(name="stack-mern", resourcePath="api-development.md")`.

Read the entry that matches what you are about to write, not all of them.

- **`api-development.md`** — Build REST APIs with proper error handling, status codes, request validation, response formatting, and rate limiting. Apply when creating API routes, 
- **`database-migrations.md`** — Design schemas, create reversible migrations, validate data at API and DB layers. Apply when designing databases, creating migrations, defining models
- **`typescript-standards.md`** — Enforce TypeScript best practices, branded types for IDs, import organization, naming conventions. Apply when writing functions, choosing type pattern
- **`nextauth-authentication.md`** — Implement authentication with NextAuth.js v5, Google OAuth, credentials provider, session management, and protected routes. Apply when building auth f
- **`session-management.md`** — Save, resume, and compact Claude Code sessions for long-running work across context windows. Apply when approaching context limits, switching tasks, o
- **`api-security-hardening.md`** — Harden REST and GraphQL APIs against common attack vectors. Apply when building API endpoints, implementing authentication, handling file uploads, or 
- **`docker-containerization.md`** — Production-ready Docker patterns for multi-stage builds, security hardening, and orchestration. Apply when creating Dockerfiles, docker-compose config
- **`monitoring-logging.md`** — Implement CloudWatch monitoring, error tracking with Sentry, structured logging, and alert configuration. Apply when setting up monitoring, tracking e
- **`tailwind-design-system.md`** — Build consistent, accessible UI components with Tailwind CSS, shadcn/ui, and custom design tokens. Apply when styling components, creating design syst
- **`accessibility-ux.md`** — Build WCAG 2.1 AA compliant interfaces with keyboard navigation, error prevention, and screen reader support. Apply when designing UI, building forms,
- **`performance-optimization.md`** — Optimize Next.js bundle size with code splitting, tree shaking, lazy loading, and build configuration. Apply when improving performance, reducing bund
