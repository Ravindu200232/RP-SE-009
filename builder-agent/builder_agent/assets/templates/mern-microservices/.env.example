# One port for the whole application. The gateway owns it.
PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/sketch_mern
# Internal service ports. Nothing outside the machine uses these.
CATALOG_PORT=4101
