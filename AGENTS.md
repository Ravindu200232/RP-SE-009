# Local execution rule

Never use Docker on this local PC: do not install, start, probe or run Docker Desktop, a Docker daemon, Docker CLI, Docker Compose, containers or local Docker image builds. Use Node/package manager tools for local previews and validation. Docker deployment artifacts may be generated, but Docker builds and pushes run only on an explicitly configured cloud CI runner.
