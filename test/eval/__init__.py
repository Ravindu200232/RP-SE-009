"""SRS Agent evaluation harness.

The repository suite under `test/unit` and `test/integration` asserts that the
code behaves; this package asserts that the *document the agent writes* holds
the properties the dataset says it should. See `test/eval/README.md`.
"""
